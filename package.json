{
  "name": "3d-restoration-evaluator",
  "private": true,
  "version": "0.1.0",
  "description": "3D Person Restoration Evaluator - Gemini-powered 5-dimension restoration quality evaluation",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "test": "npm run test -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
